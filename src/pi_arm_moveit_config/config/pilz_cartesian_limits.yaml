# Cartesian TCP limits for move_group Pilz LIN/CIRC only (SI units).
# Manager MoveL reject/scaling caps are compile-time constants in
# pi_arm_manager/src/manager_node.cpp (kMaxTransVelMps, ...). Keep both aligned.
cartesian_limits:
  max_trans_vel: 0.1
  max_trans_acc: 0.2
  max_trans_dec: -0.2
  max_rot_vel: 0.174532925
